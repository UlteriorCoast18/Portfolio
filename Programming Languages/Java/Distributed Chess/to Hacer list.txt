TO hacer del ajedrez:

-Hacer registro para la creación de socket (se colocará si se va a montar un servidor o no se montará el mismo, con ello se
decidirá quién o quién no es el host).

-luego hacer la conexión entre ambos y solo limitarla a los dos usuarios que tengo.

-Implementar métodos para que envíe y reciba el arreglo de botones, con la última posición dada y con la ultima pieza
movida y el turno que lleva.

-hacer que espere conexión de otro usuario y de allí que inicie la partida

-método de resetear y volver a jugar (c guardan parámetros y de allí solo se hace una nueva conexión y se limpia el tablero)

-método de salida segura del servidor sin que truene :u